export default class Vec3 {
    constructor(x, y, z){
        this.x = x
        this.y = y
        this.z = z

        // this.set(0, 0, 0)
    }

    set(x, y, z) {
        this.x = x
        this.y = y
        this.z = z
    }
}
